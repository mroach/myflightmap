version: '3'

services:
  app:
    environment:
      RACK_ENV: production
      RAILS_ENV: production
      AUTO_DBMIGRATE: 1
  web:
    ports:
      - 80:80
      - 443:443
    volumes:
      - certs:/etc/letsencrypt
      - certs-data:/data/letsencrypt
